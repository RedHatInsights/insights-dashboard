import { Button } from '@patternfly/react-core/dist/esm/components';
import React from 'react';

const StartYourTrialButton = () => {
    return (
        <Button style={{ paddingLeft: '32px', paddingRight: '32px' }}>
            Start your trial
        </Button>
    );
};

export default StartYourTrialButton;
